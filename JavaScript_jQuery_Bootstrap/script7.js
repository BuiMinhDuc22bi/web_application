$(document).ready(function() {
    // Change the href attribute (the hyperlink)
    $('#usth').attr('href', 'https://www.new-url.com');
    
    // Change the text of the link
    $('#usth').text('New Website');
});
